{{- range $name, $svc := .Values.services }}
{{- if not $svc.existingSecretName }}
{{- $ctx := dict "root" $ "name" $name "svc" $svc }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "order-service.fullname" $ctx }}
  labels:
    {{- include "order-service.labels" $ctx | nindent 4 }}
type: Opaque
stringData:
  {{- range $k, $v := $svc.secretEnv }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
---
{{- end }}
{{- end }}
